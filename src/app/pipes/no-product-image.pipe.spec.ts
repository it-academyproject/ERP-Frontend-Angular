import { NoProductImagePipe } from './no-product-image.pipe';

describe('NoProductImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NoProductImagePipe();
    expect(pipe).toBeTruthy();
  });
});
